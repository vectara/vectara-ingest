csv_parser:
  delimiter: "|"

doc_processing:
  model_config:
    text: {}

vectara: {}

test:
  expected_index_segments_call:
    doc_id: asdf9asdfa3
    doc_metadata:
      source: test_dataframe_parser
    doc_title: test.pipe
    tables:
      - headers:
          - first_name
          - last_name
          - description
        rows:
          - - example
            - user
            - this is an example user
          - - admin
            - user
            - this is an admin user
        summary: This is a summary.
    texts:
      - This is a summary.
  input_path:
    - data
    - dataframe
    - test.pipe
